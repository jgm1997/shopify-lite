version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries/"
    schema: "db/migrations/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        overrides:
          - db_type: "pg_catalog.numeric"
            nullable: false
            go_type:
              type: "float64"
